{
  "name": "group-digest",
  "version": "0.1.0",
  "description": "GroupDigest — OpenClaw 群聊智能摘要 + 待办提取插件",
  "type": "module",
  "openclaw": {
    "extensions": [
      "./index.ts"
    ]
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^25.3.5",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
